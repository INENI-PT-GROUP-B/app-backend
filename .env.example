# PostgreSQL connection string used by the backend (Drizzle / pg).
# The local-dev value matches the Postgres service in docker-compose.yml.
DATABASE_URL=postgresql://app:app@localhost:5432/app

# Port the Fastify server listens on (defaults to 3000 when unset).
PORT=3000
